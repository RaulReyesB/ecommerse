import { Routes } from '@angular/router';
import { ProductCardComponent } from './components/products/ui/product-card/product-card.component';

export const routes: Routes = [
  {path:"Inicio", component:ProductCardComponent }
];
